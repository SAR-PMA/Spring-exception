spring.application.name=spring-exception

# Logging
logging.level.root=INFO
logging.level.com.example.demo=DEBUG

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,logfile

resilience4j.retry.instances.itemService.max-attempts=3
resilience4j.retry.instances.itemService.wait-duration=500ms

